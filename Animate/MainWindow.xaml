<Window x:Class="Animate.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Animate"
        mc:Ignorable="d"
        Title="Animate!" Height="600" Width="1024"
        Loaded="Window_Loaded">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*"></ColumnDefinition>
            <ColumnDefinition Width="200"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="10">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Width" Value="100"></Setter>
                    <Setter Property="Height" Value="30"></Setter>
                </Style>
            </StackPanel.Resources>
            <Button Content="Load..." Click="LoadImage_Click" Margin="5"/>
            <Button Content="Clear Frames" Click="ClearFrames_Click" Margin="5"/>
            <Button Content="Show outlines" Click="ShowOrigins_Click" Margin="5"/>
            <Button Content="Hide outlines" Click="HideOrigins_Click" Margin="5"/>
            <Button Content="Export..." Click="ExportImages_Click" Margin="5"/>
        </StackPanel>

        <TextBlock Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="20" FontFamily="Verdana">Animation Stack</TextBlock>

        <!-- Canvas + Image -->
        <ScrollViewer Grid.Row="1" Grid.Column="0" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible">
            <Canvas Name="ImageCanvas" Background="BlueViolet" MouseLeftButtonDown="Canvas_MouseDown"
                    MouseMove="Canvas_MouseMove" MouseLeftButtonUp="Canvas_MouseUp">
                <Image Name="MainImage"/>
            </Canvas>
        </ScrollViewer>
        
        <!-- Canvas + Image -->
        <DockPanel Grid.Row="1" Grid.Column="1">
            <Border DockPanel.Dock="Bottom" BorderThickness="0" BorderBrush="Black" Padding="8">
                <DockPanel>
                    <Label DockPanel.Dock="Left" VerticalAlignment="Center">Frame Duration</Label>
                    <TextBox DockPanel.Dock="Right" Width="50" Text="{Binding FrameDuration, Mode=TwoWay}"></TextBox>
                    <Slider Minimum="100" Maximum="1000" Value="{Binding FrameDurationValue}" VerticalAlignment="Center"></Slider>
                </DockPanel>
            </Border>
            <Border DockPanel.Dock="Bottom" Height="200" BorderThickness="1" BorderBrush="Black" Margin="6">
                <Image Name="AnimedImage"/>
            </Border>
            <ScrollViewer VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Hidden" Margin="6">
                <ListView ItemsSource="{Binding Frames}" SelectionChanged="ListView_SelectionChanged">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{Binding Position}"></TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </ScrollViewer>
        </DockPanel>
    </Grid>
</Window>
